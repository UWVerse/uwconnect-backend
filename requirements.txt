# Essentials
flask~=2.2 # Flask
Jinja2>=3 # templating engine
requests~=2.24 # HTTP library
pymongo~=4.3.3 # MongoDB
autopep8~=1.5 # automatically formats Python code to conform to the PEP 8 style guide
faker~=16.6 # Generating dummy data
validate-email~=1.3 # check if an email is valid, properly formatted and really exists.
itsdangerous~=2.1 #Safely pass trusted data to untrusted environments and back
Pillow~=8.0 #Python Imaging Library
timeago~=1.0 # format datetime with `*** time ago` statement
argparse~=1.4 # enable command line arguments
bcrypt~=3.2 #password hashing 
Markdown~=3.4
MarkupSafe~=2.1
coverage~=7.1 # measuring code coverage
decorator~=4.4
defusedxml~=0.6 # preventing XML bomb security vulnerabilities
WTForms~=3.0 # flexible forms validation and rendering library 
grip~=4.6 # Render local readme files
guess-language~=0.2 #detect the language of a given string
pytz~=2020.1 #timezone calculations
speaklater~=1.3 #lazy strings
sqlparse~=0.4 #SQL parser
mongoengine~=0.26.0

# Flask extensions
flask-cors~=3.0 #for handling Cross-Origin Resource Sharing
flask-mail~=0.9 #for sending emails
flask-swagger-ui~=4.11 #Flask blueprint for adding Swagger UI
flask-login~=0.6 #user session management
flask-Bcrypt~=1.0 #bcrypt hashing utilities
flask-Bootstrap~=3.3 #Bootstrap with flask
flask-WTF~=1.1 #WTForms library with Flask
flask-RESTful~=0.3 #RestfulAPI
flask_pymongo~=2.3